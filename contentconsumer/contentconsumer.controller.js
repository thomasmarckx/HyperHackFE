(function () {
    'use strict';

    angular
        .module('app')
        .controller('ContentConsumerController', ContentConsumerController);

        ContentConsumerController.$inject = ['$rootScope'];
        function ContentConsumerController($rootScope) {

        }
})();
